//! peers-client — servidor MCP stdio de la red claude-peers-rs.
//!
//! (Placeholder de scaffold — se rellena con el protocolo MCP + la notificación de canal
//! en la siguiente fase, una vez confirmado el contrato exacto de push.)

fn main() {
    eprintln!("peers-client: pendiente de implementación");
}
